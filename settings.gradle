rootProject.name = 'nilou'

include ':api'
include ':testing'
include ':processor'

def setBuildFile(project) {
    project.buildFileName = "${project.name}.gradle"
    project.children.each {
        setBuildFile(it)
    }
}

setBuildFile(rootProject)
rootProject.buildFileName = "build.gradle"